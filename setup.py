from setuptools import setup

setup(
    name="bearychat",
    version="0.1.0",
    description="SDK for BearyChat",
    url="bearychat.com",
    install_requires=[
        "requests"
    ],
    packages=["bearychat"])
